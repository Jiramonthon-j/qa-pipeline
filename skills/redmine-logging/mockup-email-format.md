# Mockup — เนื้อหาอีเมลแจ้งเตือน Developer (ขั้นตอนที่ 6)

> **สถานะ: ยืนยันแล้ว เขียนลง SKILL.md และ `scripts/send_email_notification.py` (คำสั่ง `build-body`) จริงแล้ว**

## จุดที่ confirm ไปแล้ว

**1. "[P1 - High]" — คำ Severity ท้าย Priority**: ยืนยันใช้ mapping มาตรฐาน P0=Critical, P1=High, P2=Medium, P3=Low (เขียนเป็นค่าคงที่ `PRIORITY_TO_SEVERITY` ในสคริปต์แล้ว)

**2. "Test Run Date"**: ยืนยันใช้ **วันที่รัน Skill 09 จริง** (วันที่เปิด Ticket/ส่งอีเมล) ไม่ใช่วันที่ใน `08-qa-report.docx`

**3. ย้าย [Module] ออกมาเป็นบรรทัดของตัวเอง**: ยืนยันแล้ว ใช้ field ชุดเดียวกับรายงาน Terminal/Excel export (Module/Test Case/Priority/Description)

## จุดที่ปรับ layout (ไม่ใช่คำถาม แค่แจ้งเหตุผล)

Format ต้นฉบับใส่ `[Module]` ไว้ในบรรทัดหัวข้อบั๊กด้วย (เช่น `[Auth] Special characters...`) และใส่ชื่อสั้นๆ ไว้ในวงเล็บท้าย Test Case (เช่น `TC-003 (Special Character Validation)`) — Workbook ของ Pipeline นี้ไม่มี field "ชื่อสั้น" แยกจาก Module อยู่แล้ว เลยเสนอย้าย Module ออกมาเป็นบรรทัดของตัวเอง (`• Module : ...`) แทนที่จะยัดไว้ในหัวข้อ/วงเล็บ — ก็จะได้ field ชุดเดียวกับที่ใช้ในรายงาน Terminal และไฟล์ Excel export ทำให้ทั้ง 3 output (Terminal/Excel/Email) ใช้ชื่อ field ตรงกันหมด อ่านสลับไปมาไม่งง

## ตัวอย่างเต็ม — ใช้ TC-017 (ข้อมูลจริง) + TC-009 (เคสสมมติ เพื่อสาธิตกรณีมีหลายเคส)

(ใช้ mapping Severity ที่เสนอไว้ข้างบน และ Test Run Date = วันที่รัน Skill 09 เป็นตัวอย่างไปก่อน รอ confirm)

```
Hi Dev Team,

ระบบ QA Automation ได้ทำการทดสอบระบบล่าสุดเสร็จสิ้น และพบ Bug ที่ต้องการการแก้ไขจำนวน 2 รายการ
รายละเอียดและลิงก์สำหรับเข้าดู Ticket ใน Redmine ถูกสรุปไว้ด้านล่างนี้ครับ:

================================================================================
🐞 DEFECT SUMMARY REPORT
================================================================================
Test Run Date: 01 Sep 2026 | Environment: Staging (mock) — ทดสอบผ่าน Chromium
--------------------------------------------------------------------------------
1. [P1 - High] ใช้โค้ดส่วนลดเปอร์เซ็นต์ซ้อนกับสินค้าที่ลดราคาอยู่แล้ว
--------------------------------------------------------------------------------
• Issue Link : https://redmine.example.com/issues/1025
• Module     : ตะกร้าสินค้า - คำนวณราคา
• Test Case  : TC-009
• Description: ราคาสุทธิคำนวณผิด ส่วนลดซ้อนเกินกว่าที่ควรจะเป็น
--------------------------------------------------------------------------------
2. [P2 - Medium] ลบโค้ด A แล้วใส่โค้ด B ต่อทันที — กรณีสมมติฐานว่านับสิทธิ์โค้ด A
   ทันทีตอนกด "ใช้โค้ด" ครั้งแรก
--------------------------------------------------------------------------------
• Issue Link : https://redmine.example.com/issues/1024
• Module     : ตะกร้าสินค้า - โค้ดส่วนลด
• Test Case  : TC-017
• Description: ระบบนับสิทธิ์การใช้โค้ด A ทันทีที่ลบออก ทั้งที่ไม่เคย checkout สำเร็จด้วยโค้ด A เลย
• Steps to Reproduce:
  1. กดปุ่ม "ลบโค้ด" สำหรับโค้ด A (ONETIME10, usage_limit_per_user=1, ใช้งานอยู่ในออเดอร์นี้แล้ว)
  2. กรอกโค้ด B (FLAT100)
  3. กดปุ่ม "ใช้โค้ด"
  4. ตรวจสอบสิทธิ์การใช้โค้ด A ที่เหลือของผู้ใช้คนนี้ในภายหลัง
================================================================================
📁 Attached Evidence:
- สามารถดู Path ของไฟล์ Screenshot ผลการทดสอบได้ที่หัวข้อ "หลักฐาน" ใน Description ของแต่ละ Ticket (อ้างอิง path ในระบบไฟล์ของทีม QA — Skill นี้ยังไม่ได้อัปโหลดไฟล์แนบขึ้น Redmine โดยตรง)
- สามารถคลิกที่ Issue Link ด้านบนเพื่อดูรายละเอียดเพิ่มเติมและอัปเดต Status ได้ทันทีครับ

Best regards,
QA Automation Assistant
--------------------------------------------------------------------------------
```

**หมายเหตุ**: บั๊กที่ 1 (TC-009) ไม่มี "Steps to Reproduce" เพราะเป็นเคสสมมติที่ไม่ได้เตรียมข้อมูลไว้ครบ — ในของจริง TC-009 นี้ไม่ได้อยู่ใน Workbook เลย เอามาแสดงเฉพาะให้เห็นหน้าตาตอนมีมากกว่า 1 เคส เท่านั้น (บั๊กเรียงตาม Priority ก่อนเหมือนรายงาน Terminal — P1 มาก่อน P2)

**เรื่อง "Steps to Reproduce" ในอีเมล**: ตอนนี้ cases-json ยังไม่มี field แยกสำหรับ Steps (อยู่ปนอยู่ในข้อความเต็มของ `description` ที่ส่งเข้า Redmine เท่านั้น) ถ้ายืนยัน Format นี้ ต้องเพิ่ม field `steps` (list ของข้อความทีละ step) เข้าไปใน cases-json schema ด้วย เพื่อให้สคริปต์ดึงมาใช้ประกอบอีเมลได้โดยไม่ต้องแกะจากข้อความเต็ม
